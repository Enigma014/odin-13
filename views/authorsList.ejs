<h1>Authors List</h1>
<% if (authors.length === 0) { %>
  <p>No authors found.</p>
<% } else { %>
  <ul>
    <% authors.forEach(author => { %>
      <li><%= author.name %> (Born: <%= author.birth_year %>)</li>
    <% }); %>
  </ul>
<% } %>
